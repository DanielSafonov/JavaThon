spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/javathon?currentSchema=javathon_schema
    username: postgres
    password: postgres

  jpa:
    properties.hibernate.temp.use_jdbc_metadata_defaults: false
#    properties.hibernate.dialect: org.hibernate.dialect.PostgreSQL96Dialect
  mustache:
    expose-request-attributes: true
server:
  port: 8081


